<script lang="ts">
  /**
   * Badge — colored pill for displaying statuses and labels.
   *
   * variant controls the color:
   *   green  → ACTIVE, PROCESSED, HEALTHY
   *   yellow → PENDING, MEMBER
   *   red    → INACTIVE, FAILED, DISCREPANCY
   *   blue   → WEB
   *   purple → COLLECTOR, WHATSAPP
   *   gray   → default fallback
   */

  type Variant = 'green' | 'yellow' | 'red' | 'blue' | 'purple' | 'gray';

  let {
    text,
    variant = 'gray',
  }: {
    text:     string;
    variant?: Variant;
  } = $props();

  // Map variant name to Tailwind classes
  const styles: Record<Variant, string> = {
    green:  'bg-green-100 text-green-700',
    yellow: 'bg-yellow-100 text-yellow-700',
    red:    'bg-red-100 text-red-700',
    blue:   'bg-blue-100 text-blue-700',
    purple: 'bg-purple-100 text-purple-700',
    gray:   'bg-gray-100 text-gray-600',
  };
</script>

<span class="inline-flex items-center px-2.5 py-0.5 rounded-full
             text-xs font-medium font-mono {styles[variant]}">
  {text}
</span>